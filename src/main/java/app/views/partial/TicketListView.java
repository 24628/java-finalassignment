package app.views.partial;

import app.database.Database;
import app.views.BaseListView;

public class TicketListView extends BaseListView {
    public TicketListView(Database database) {
        super(database);
    }
}
